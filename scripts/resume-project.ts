// Compatibility entry point named by the implementation plan.
// The maintained command is `pnpm project:resume`; this wrapper preserves the
// plan's script path while sharing the same fail-closed implementation.
import "./project-resume";
