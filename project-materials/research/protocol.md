# 正式研究协议（模板）

状态：`WAITING_EXTERNAL_INPUT`。

正式扫描前由两位负责人填写并共同冻结：研究问题、分析单位、网站类别定义、每类目标数量、页面上限、采集时间窗、纳入/排除规则、预排替补、失败处理、`accesscheck-score-v1`、`manual-review-sampler-v1`、验证方法、WCAG 2.2 与 GB/T 37668-2019 的方法边界。

本文件是空白协议，不含正式站点、正式分数或人工结论。R1 receipt 必须绑定它的实际 SHA-256；之后的执行日志不得回写本协议。
