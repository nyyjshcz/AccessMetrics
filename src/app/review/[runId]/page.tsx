import ReviewWorkbench from "@/components/review-workbench";

// Keep the original reviewer URL working while the result page now links to
// the clearer /scans/[runId]/review workbench.
export default ReviewWorkbench;
