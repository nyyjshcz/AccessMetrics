# 故障处理速查

> **历史参考。** 本文对应旧运行方式；当前故障检查和启动方式见 [运维说明](../../../operations.md)。

先看 `requestId`、job/run 状态、`last_error_code` 和 Worker lease；不要手改状态或删除 attempt。数据库问题先备份并恢复到新文件；manifest/hash 不符时停止发布；真人门缺失时运行 `pnpm project:resume` 会保持等待。
