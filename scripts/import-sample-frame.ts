// Compatibility entry point named by the implementation plan.
// The maintained command is `pnpm study:import`; this wrapper keeps the
// documented script path executable without creating a second importer.
import "./study-import";
