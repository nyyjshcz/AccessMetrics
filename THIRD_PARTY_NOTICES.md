# 第三方组件声明

本项目使用的第三方组件及许可证以 `pnpm-lock.yaml` 和各 npm 包发布的许可证为准，包括 Next.js、React、Playwright、axe-core、better-sqlite3、Zod、Pino、Vitest、Prettier、TypeScript、docx 与 fflate。发布前运行依赖清单、锁文件和容器镜像 SBOM 检查，并将结果写入私有 release validation 证据；本文件不替代上游许可证文本。
