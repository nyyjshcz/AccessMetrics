# 两位负责人理解检查题库

1. 一个 axe violation 如何保存 impact、规则、WCAG、节点和清理后的证据？
2. 为什么 incomplete 不直接扣分？
3. 分数的分母为什么是 pass+fail 检查机会而不是 DOM 总数？
4. URL 经过 DNS 返回一个公网和一个内网地址时系统做什么？
5. Worker 在页面处理一半时崩溃，重启如何恢复且不重复写结果？
6. 一个 export-id 如何回溯到 run、页面、规则、原始 manifest？

每题必须能指出代码路径、数据库表和可运行测试；六题中任一题不合格，都不能把理解门标为通过。
