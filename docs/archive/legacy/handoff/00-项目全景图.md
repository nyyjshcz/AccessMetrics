# 项目全景图（空白接手包）

> **历史参考。** 这份接手包对应旧方案。当前入口请读[旧交接材料说明](./README.md)。

入口是 `/` 和管理/审核页面；核心链路是 URL 安全 → crawler → axe → SQLite → score → export/report。R1–R5 证据在 Git 外私有根目录；公开仓库只保留方法、代码和 hash 索引。接手人必须用自己的 reviewer 会话完成练习，不能把阅读本文件当作确认。
