# 评分解释

每个原则分数 = `100 × (原则权重总量 − 加权缺陷) / 原则权重总量`。严重程度只决定节点缺陷权重（4/3/2/1），不是“网页好坏”的主观评价。一个规则映射到多个原则时，该节点对每个映射原则分别计入，但网站总分按规则节点的唯一归属/模型配置避免重复放大。`incomplete` 会显示覆盖和待核验数量，但不扣分；frame 未覆盖会显示 `coverage_limited`，不能解释为全页面通过。

所有分子、分母、十倍分数和模型版本都进入数据库和 manifest；前端的 0–100 防御性显示不能替代权威整数值。
